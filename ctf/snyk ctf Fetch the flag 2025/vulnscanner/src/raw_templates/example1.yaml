name: Basic HTTP Check
description: A template to check for HTTP responses.
type: http
requests:
  - method: GET
    path:
      - "/"
    matchers:
      - type: status
        status:
          - 200
